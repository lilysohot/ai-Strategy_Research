# 2026-09-18

## I2-5（publication_pg 真库门）偏离与缺点复核

用户要求对照 tasks.md / 架构 §8.1 检查 I2-5 是否偏离。结论：**主体验收门达标，但有两处实质缺口**。

时点澄清（重要）：当前工作区的 `INDEX_REV_V3` / `chunk.normalize_search_text` / `search_pg` 查询侧
归一化属于 **i0c-r7（R1—R6 审核整改，2026-09-18 创建）**，**不是 I2-5 的交付**；I2-5 交付对应 **i0c-r6**
（task 字段明确为 "I2-5 publication_pg"）。评估 I2-5 以 r6 时点为准。
另：`i0c-r6` 与 `i0c-r7` 里 `test_corpus_preparation_publication_pg.py` 哈希不同
（09e9d8c4… vs 3978ccc6…），即 R5 整改改动了 I2-5 测试族。

- **覆盖核对**：tasks.md 8 项（双 worker/过期 token/提交丢响应/撤销/取消/并发发布/失败恢复/
  配置升级）**全覆盖**（13 用例）；架构 §8.1 的 7 必验场景覆盖 **6/7**，缺**"连接断开"**。
  验收门达标：真库 31 passed 无一 skip；真线程 + 每 worker 独占连接 + advisory 锁 + 行锁，无内存锁替代。
- **缺点 1（最严重）MemoryStore fencing 次序未与 PgStore 同步**：台账修复③声称"凭据不匹配先判
  lease_lost、双 Adapter 同步收敛"，实际次序相反——PgStore `_require_ownership` 是
  `owner/token → 非 running → 过期`；MemoryStore 的 `_require_stage_ownership` /
  `_require_publication_ownership` / `_fence` 三处全部是 `非 running → owner/token → 过期`。
  后果：worker 被接管且 job 已 succeeded 后旧 token 迟到写入，PG 返回 lease_lost、
  内存返回"处于 succeeded 态、无当前所有权"。违反架构 §9 双 Adapter 同 Seam。
  （修复④ max_attempts 确实同步了，只有③次序没同步。）
- **缺点 2 heartbeat 续租成功路径全仓零覆盖**：全仓搜 `heartbeat_job` 在 tests/ 下仅 2 处且**都是
  失败路径**（contract.py:408 token 不匹配、publication_pg.py:387 过期）。架构 §8.1.3 的"续租"正向
  行为（lease_until 前移）从未验证——架构把"**续租**/过期恰好临界"列为 I2 必验项，I2-5 只覆盖"过期"半边。
  风险：heartbeat UPDATE 若写错（未更新 lease_until 或误用调用方时钟）现有测试全不红。
- **缺点 3**：架构 §8.1 明列的"连接断开"场景无用例；`_backdate_leases` 是记账式改写 lease_until，
  非连接断开。（`test_live_*` 用 time.sleep 真实停顿越过 TTL 是对的，符合 I0C-2 计时证据要求。）
- **次要**：`chunk_rev` 仍 `chunk-2` 未随 `normalize_search_text`（新增全角 ％）联动，而 engine.py
  注释说"索引文本规则随 chunk_rev 绑定"——只看 chunk_rev 察觉不到 search_text 规则已变（正确性无碍）。
- **待确认**：r7 整改后 I2-5 的 13 用例是否仍全绿（r7 重绑了该测试文件但未见复跑证据）。

## I2-5 独立复核整改（RM-1～RM-13）已实施完成

依据 `audits/20260918-i25-review/review.md` + `remediation-checklist.md`。探针基线 4 failed/1 passed
→ 整改后 **5 passed**。I2-5 状态：首轮 complete → 0918 复核判定「实现+首轮测试已交付、整项验收
未达标」→ 本批次闭环后满足 DoD 1—5。**M5 仍 not_declared**。

**批次 A（代码缺陷，RM-1～RM-4）**
- **RM-1** `finish_job` 终态短路先于所有权校验：PG(`repository_pg.py`) + Memory(`repository.py`)
  两侧均改为**先锚定所有权再判终态**（fence_token 唯一绑定 attempt）→ 不一致即 `lease_lost`，
  一致才是合法重放。修复前：接管者完成 attempt N+1 后，旧 worker 携 attempt N 旧 token finish
  被误认成功。
- **RM-2** 指针已切、finish 未提交即断线：新增 `engine._reconcile_published_job`，短路返回前补齐
  PUBLISHED 终态。选**受 fencing 保护的幂等恢复**（非合并事务）：先用当前行租约提交→因过期被拒则
  重取租约后提交→租约仍被他人持有（未过期）时**不抢占**。保持 Store Seam 职责、不扩 J1/J2 锁范围。
- **RM-3** 引擎短路绕过当前准入：新增 `engine._verify_admission_still_current`（决定仍是来源当前
  决定 且 仍 in_scope）。**拒绝抛 `StoreError` 而非 `EngineError`**——关键：EngineError 继承
  ValueError，而探针与 RM-3「与 Store.publish 事务内拒绝同协议」要求 RuntimeError 家族。
- **RM-4** 双 Adapter fencing 次序统一：Memory 三处（`_require_stage_ownership`/
  `_require_publication_ownership`/`_fence`）由「状态优先」改「凭据优先」；两侧「租约已过期」
  消息统一含 `lease_lost`。新增双 Adapter 对称用例断言同一错误语义。

**批次 B（真 PG 补测，RM-5～RM-8）**
- RM-5 续租正向（此前全仓零覆盖）：`lease_until` 前移/不换 token/不耗 attempt/过期不原地续命。
- RM-6 连接断开（区别于「提交响应丢失」重放）：无半提交 + 撕裂态可补齐 + 指针/generation 不漂移。
- RM-7 `put_admission` 取 `pg_advisory_xact_lock(pub_ns, source_id)`，与 publish/retire 同临界区。
- RM-8 引擎级恢复：走 `publish_build` + `_verify_publication_ready`(PARSED/CHUNKED 门)，
  而非 `_publish_chain` 直操作 Store。

**批次 C（收口）**
- RM-10：明确 search_text 规则**由 `index_rev` 独立表达**、不随 `chunk_rev`（前者只改 token 形态、
  后者改块边界）；修正 engine.py 注释。`CHUNK_REV` 保持 chunk-2。
- RM-12 回填台账+任务清单；RM-11 新建 **i0c-r8**（parent=r7），`validate_i0c_freeze.py` **exit 0**
  （血缘 r8→r7→…→r1→i1-r4→i1-r3→i1-r1→i0a5(M1)）。
- **RM-9（测试卫生：daemon 线程/statement_timeout）P3 未实施**，已在 i0c-r8 notes 登记理由。

**踩坑记录（可复用）**
- 断连桩只查 `kwargs.get("state")` 会失败：`finish_job`/engine 均以**位置参数**调用，state 在 `args[5]`。
- MemoryStore 的 `put_units` 不接受 `now`，过期判定走 `self._clock()` → 测试须 `MemoryStore(clock=lambda: NOW)`，
  否则真实墙钟会让模拟时间的租约看起来已过期。
- 守卫自检 `test_corpus_preparation_guard.py` **不能在 i1 守卫 env 下跑**（父进程已装 i1 守卫，
  子进程拒绝换配置重装 i0-inventory），按 M4 口径在普通环境跑（19 passed）；`--ignore` 在
  `--noconftest -c /dev/null` 下不生效，须显式列出文件。

**验证证据**：探针 5 passed；真库门 publication_pg+repository_pg **35 passed 零 skip**；
普通环境 214 passed/2 skipped；i1 守卫 env 业务 12 文件 195 passed；守卫自检 19 passed；
ruff check plugins/ All passed + format 18 files ok；pyright 0 errors；import_smoke 356/356。

## 前情：I2-3/I2-7 独立审核报告与我上一轮复核的差异（已确认）

审核报告 `audits/20260917-i23-i27-review/review.md` 用最小反例实测，纠正了我三处判断：
① % 归一化我当成优点，实测是**漏召回缺陷**（写侧规范化、查询侧不同步）；
② 我把 `probe_report_publication` 当"正确唯一落点"，实测它也有 bug（文件名日期经 descriptor.title
拼入 + 正文事件日期误当报告日期）；③ coverage 三轴归 I2-8，不判 I2-3 偏离（我判重了）。
报告独有、我遗漏的：R3（按源字节短路忽略新决定/版本）、R6（冻结漏绑 publication.py 及其测试）、
R2 里写读 DSN 不统一（写走 CORPUS_I2_DSN、读走 self._dsn）。

## I2-5 完整回溯（2026-09-18 第二轮，含实测复跑）

发现并采用既有独立复核 `.scratch/.../audits/20260918-i25-review/`（review.md + test_review_probes.py，
创建于 r7 之前 00:14）。本轮实测（宿主直接验证）：

- 真库门复跑：i2-sandbox 守卫 env，`publication_pg.py` **13 passed**；加 `repository_pg.py` **31 passed 无 skip**。
- `validate_i0c_freeze.py` **exit 0**（血缘 r7→…→i0a5 全过）——上轮担心的 F4 已因 r7（00:24 创建，
  绑定当前文档 4d89c869）自愈，**当前不复现**。
- 复核探针复跑：**4 failed / 1 passed**，F1—F3 当前可复现。
- 台账/清单 **均未回填** 0918 复核（plan 与 tasks 各 0 处提及），I2-5 仍记 `complete`。

**F1（P1，比上轮"次序不一致"更严重）**：`finish_job` 的终态幂等短路发生在 owner/token 校验**之前**——
`repository_pg.py:1078-1085`、`repository.py:523-527`。接管者已完成 attempt 2 后，旧 worker 用 attempt 1 的
旧 token 提交 succeeded 会拿到"成功"返回，而非 lease_lost。C7 第 4 项"旧 worker 迟到 finish"因此漏测
（测试 2 只在接管者仍 running 时查旧 finish；测试 3 只查 put_units/put_chunks/publish）。

**F2（P1）**：`engine.publish_build`（`engine.py:1147-1153` 短路 + `1184` finish）把"指针切换"与
"PUBLISHED 终态"分成两次 Store 调用；中间断线后重试被幂等短路直接返回，PUBLISHED job 永远 RUNNING。

**F3（P1）**：同一短路绕过 `_verify_publication_ready` 与当前准入校验 → 准入已改为排除但未 retire 时，
引擎重试仍返回 success（store.publish 有校验，但引擎短路走不到）。

**仍需补的真 PG 场景**：①`put_admission` 未取 `_ADVISORY_PUB_NS` 锁，准入更新×publish 可交错；
②锁等待跨 TTL / heartbeat 正向续租；③真实引擎全链恢复（现 13 用例用 `_publish_chain` 直操作 Store、
手工造 chunk，绕过 VERIFIED 质量与引用门）；④连接断开、statement_timeout/lock_timeout 与线程收尾。

**结论**：tasks.md 8 步名义全覆盖但含上述空洞；架构 §8.1 七项必验覆盖约 4.5/7。I2-5 应改
`partial · 独立复核待整改`，M5 保持 not_declared。J1/J2 修复（advisory 锁）有价值、不需推翻。

### 产出：整改清单文档（2026-09-18，用户只要文档、未动代码）

落点 `.scratch/corpus-evidence-pipeline/ingestion-rebuild/audits/20260918-i25-review/remediation-checklist.md`
（draft，非执行授权）。结构：§0 放行口径 → §1 总表 RM-1～RM-13 → §2 逐项（位置/现状/整改/必增回归用例/不动项）
→ §3 批次顺序 A 代码缺陷 / B 真 PG 场景 / C 重冻+回填 → §4 明确不做 → §5 关闭判据 → §6 复现命令。

RM 编号：RM-1 finish 短路、RM-2 断线恢复、RM-3 短路绕过准入（三项 P1 阻断 I2-5）、RM-4 双 Adapter 次序、
RM-5 续租正向、RM-6 连接断开、RM-7 put_admission 互斥、RM-8 引擎全链恢复、RM-9 测试卫生、RM-10 chunk_rev 联动、
RM-11 重冻修订、RM-12 台账回填、RM-13 F4 已关闭。

**约定**：项目审计证据/复核/整改清单统一放 `.scratch/corpus-evidence-pipeline/ingestion-rebuild/audits/<日期>-<名称>/`；
相对路径从该目录回仓库根需 5 层 `..`。

## I2-8 + I2-4 执行完成（2026-09-18）

- **I2-8 消费者接线（二）**：新增 `plugins/corpus/preparation/read_pg.py` 作为读侧唯一权威入口——
  句柄 `cv2:<build_id>` + `chunk:<chunk_id>`（旧句柄/跨 build/坏哈希 `archive_required` 拒绝、
  来源撤销 `WithdrawnError`）、跨发布取回原 build（不静默换正文）、§7.3 `coverage_snapshot` 三轴；
  `service.py` 加 `read_chain()`（`CORPUS_READ_CHAIN=new|legacy|auto`，auto=有 corpus schema 即 new）
  并迁 search/fetch/`fetch_verbatim`/document_text/source_resolver/coverage；`search_pg` 命中加
  snippet/published；tools 返回版本句柄 + coverage 对象（空命中改述"当前已发布范围无匹配"）；
  `audit.py` 新增 `audit_corpus_chain`（活动版本决定/执行台账/引用闭合，纳入 run_audit 冲突）。
  golden 按 design-review 归 I3-5，未迁移。
- **I2-4 CLI 六职责**：新增 `plugins/corpus/cli.py`（plan/build/check/publish/status/rebuild-plan）。
  复用公开门：新增 `engine.check_build_publishable`（与 publish 第 3 步同一实现）与
  `engine.current_revs()`（rebuild-plan 用同一公式重算 parse_rev，不比较字面量——注意
  engine 写的 clean_rev/chunk_rev 是 `canonical_fingerprint([REV])`，不是字面量）。
  `publish` 必填 `--operator`，操作者/generation 落 PUBLISHED job 检查点 `publish-record-1`
  （`publish_build(operator=…)`，不新增表/列）。退出码 0/2/3/4/5；StoreError 以"拒绝"前缀路由到 3。
- 证据：真库（i2-verify 守卫 env）**50 passed 零 skip**；普通环境语料全量 **424 passed / 1 skipped**；
  i1 守卫 env **188 passed**；ruff CI 范围 All checks passed、pyright `plugins/corpus` 0 errors、
  import_smoke 358/358；冻结 **i0c-r9**（parent=r8）+ **i0c-r10**（仅修正台账测试计数 423→424，
  重绑 docs+validator）；证据矩阵 `audits/20260918-i2-8-i2-4/verification-matrix.txt`。
- 契约更新（非放宽）：`tests/test_corpus_coverage.py` 的 `coverage == "none"` 断言按 §7.3 改为
  coverage 对象（`query_status=no_match` + `availability=unknown`）——旧断言属旧契约。
- 环境坑复述：守卫 env 跑测试必须 `env -u PYTHONPATH`（宿主 IDE sitecustomize 补丁 shutil.rmtree
  → 派生 node 子进程被守卫拒）；`pytest --noconftest -c /dev/null` 下 TRUNCATE 必须写成
  `TRUNCATE t1, t2, ...`（不能 join 多个 TRUNCATE 前缀）。
- 下一步：**I2-6（authority + cli_isolation）**，前置 I2-4/8/5 已齐；M5 待 I2-6 完成后独立复核放行。

## I2-8 / I2-4 独立复核（2026-09-18，本轮我做，探针驱动）

方法（上轮学到的）：最小失败信号 + **阳性对照**。探针落点
`audits/20260918-i2-8-i2-4-review/test_review_probes.py`；i2-verify 守卫 env + CORPUS_I2_DSN。
结果：**5 failed / 1 passed**（对照 pass）→ 红灯构成证据。同时官方门复跑
`test_corpus_consumers_pg.py + test_corpus_cli_pg.py` = **15 passed**（未 skip），
执行方证据矩阵声明 50 passed（四族）。

**I2-8 缺口（探针/代码/design-review 三源交叉）**
1. **R1（P1）文档级读取静默换正文 + 出处错标**：`read_pg.fetch_document`（read_pg.py:212-252）
   `_DOC_SQL` 读 `publications.active_build_id`（活动 build B），但 `DocumentEvidence.build_id`
   仍填句柄的 A。实测：句柄 `cv2:<A>` 时 `fetch_verbatim` 给 A、`document_text` 给 B。
   design-review 对「文档级/活动版本/document_text/source_resolver」**零明文裁决**（grep=0），
   故不是获准例外，而是实现自选 → 违反 tasks.md I2-8「新句柄可跨发布取回原版本/不静默换正文」。
   影响 `service.document_text` → `verify.source_resolver`（溯源闸）。
2. **R2（P2）撤销后文档级返回空串**：撤销后 `document_text` 返 `""`（块级抛 `WithdrawnError`）。
   空串使"已撤销"与"空文档"不可区分；verify 路径表现为误导性"引用不符"（fail-closed 但原因错）。
3. **R3（P2）coverage 缺 §7.3 必带 `publication_snapshot_ref`**：§7.3 与 consumer matrix [11] 均列该 ref，实现只给两个 ref。
4. **R4（P2）consumer matrix [11] data_coverage.py 未落地**：design-review `migrate` 明列 data_coverage
   迁"§7.3 coverage 三轴对象"，实测仍返回旧 research/quote/financials/web 结构（research 计数已走新链，
   但三轴对象未透出）。I2-8 证据矩阵未提该文件。
5. **R5（P2）corpus_fetch 未透出 span**：matrix [4] 要求"原文片段+span/cell"，`ChunkEvidence.source_ranges`
   已算但工具载荷丢弃（实测载荷键无 source_ranges/span）。
6. **R6（P2）「旧引用归档策略」未成文**：tasks.md I2-8 明列该步；design-review:352 定义保留件为
   "doc_id→source_id 映射 manifest 供旧引用解释"，全仓无生成代码、无产出时点/所有者，仅有 `archive_required` 拒绝。
7. **R7（P3）golden 顺延**：matrix [6] stage="I2-8/I3-5"，实际完全顺延 I3-5（**有据**，但 I2-8 交付未显式登记 carry-over）。
8. **R8（P2，需裁定）`CORPUS_READ_CHAIN=legacy/auto` 保留旧 blocks 读路径**：tasks.md §3.5 明写
   "M5 不接受'新 CLI 通过，但旧 search/fetch 仍读旧 blocks'"；官方测试只用 `new` 钉住，guard 未拒绝 legacy。

**I2-4 缺口**
9. **R9（P2）CLI 复制引擎公式**：`cli.py:375-377` 内联 parse_rev 公式（用 `extractor_rev_for(source.format)`），
   引擎 `engine.py:779-781` 用 `reader_result.extractor_rev`。当前等价但违反"CLI 不复制规则"；测试仅覆盖 MD。
10. **R10（P2）同一前置条件两个退出码**：`check --build <不存在>`=4、`status --build <不存在>`=5（实测）。
11. **R11（P3）argparse 错误抛 SystemExit(2)**，不经 `main()` 返回码。

**判定**：I2-8 主体接线成立（句柄/块级跨发布/旧句柄拒绝/撤销拒绝/audit 均真库实测），但含 R1—R6 →
应 `partial · 独立复核待整改`，不得记 complete；I2-4 六职责与复用纪律基本达标，R9/R10 需收口（非阻断）。
**M5 不可放行**（R8 口径未定，R1 影响 verify 溯源闸）。

### 产出：两份正式文档（2026-09-18，已落盘并校验链接）

- `audits/20260918-i2-8-i2-4-review/review.md`：F1—F11 编号体例；含审核范围/方法声明、已成立部分、
  §6 覆盖核对（consumer matrix 逐行 + `i0c-r9` 绑定交叉 + tasks.md 验收门逐条）、§7 两项裁定项、
  §8 未覆盖项、§9 复现命令、§11 证据边界。
- `audits/20260918-i2-8-i2-4-review/remediation-checklist.md`：沿用 I2-5 的 RM 体例，编号 **`RM-I28-*`**
  （与 `RM-1`～`RM-13` 两个独立命名空间，文档内已声明不得混引）；RM-I28-0 为规格裁定前置门，
  RM-I28-1（P1）阻断 I2-8，RM-I28-8 为 M5 认定项，RM-I28-12 为重冻+回填收口。

**关键交叉证据（可复用）**：`i0c-r9` 的 `implementation` 绑定 9 文件，**未含 `data_coverage.py` 与 `verify.py`**
——冻结绑定清单本身即可佐证「哪些消费者本轮实际被触碰」，与 consumer matrix 逐行判定互为印证。
consumer matrix 中 stage 含 I2-8 的 7 行：明确达标 3 / 含缺陷 2 / 未落地 1 / 顺延未登记 1。
**批处理调用方（scripts/ 三个脚本）在 I2-8 证据中完全缺席**，已立 RM-I28-13 要求裁定归属。

## I2 全链路复核（2026-09-18，第三轮；I2 全部完成后）

方法：`diagnosing-bugs` 的紧回路 + `codebase-design` 的 seam/depth 视角。回路落点
`audits/20260918-i2-fullchain-review/test_fullchain_probes.py`（10 项：9 链不变量 + 1 出口探针），
i2-verify 守卫 env + CORPUS_I2_DSN，用**上一步真实产物**驱动下一步（不手工造 Store 行）。

**基线（全部实测）**：官方 I2 六族真库门 **71 passed 零 skip**；我的全链路回路 **9 passed / 1 failed**；
上一轮 5 红探针现已 **6 passed**（整改真闭环）。已成立的链不变量 9 条：单位级逐字性、数字/百分比跨
写侧归一化→检索→取证一致、发布后无残留 running job、重跑幂等（同 build_id/generation）、
撤销后五处口径一致（检索零候选+取证/文档级 Withdrawn+coverage withdrawn_sources+audit 无冲突）、
篡改权威后被拒、新库 legacy 降级不可达、跨发布句柄稳定、命中与 coverage 同快照含 publication_snapshot_ref。

**新发现（本轮唯一实质缺口，阻断 I3 而非 I2 本身）**：
- **F1（P1，契约偏离）§7.3 的 `scoped` 语义在实现上不可达**。架构 §344 允许「子集内无未决必需区域
  即输出 scoped」；实现 `engine._verify_publication_ready` 是「gap_regions 非空即拒绝」，
  且 `_apply_scope_to_clean` 明文「合成缺口区域（ordinal=None）保留原样：质量门拦截」→
  无坐标的缺口永远无法被判为"范围之外"，恒阻断。受影响 8 个缺口码（`_SYNTHETIC_ISSUE_STATUS`），
  含 **`empty_page`（PDF 空白页）**、`image_only_page`（扫描页）、`table_extraction_failed`、
  DOCX `unreadable_element`。实测：受批合成 DOCX 夹具（含 cell 图）build 成功但 publish 恒 exit 4。
  → 真实研报多含图表/空白页，tasks.md I3-1「三类各≥2 份 E2E 到 publish」有实质阻断风险。
- **F2（P2，契约偏离）**：架构 §9 CLI 表规定 `corpus-status` 须「显示阶段、失败、**缺口**与可执行
  恢复路径」；实测 status 载荷无缺口字段，check 拒绝时只有自然语言 error（探针红）。
- **F3（P3，保真语义）**：`DocumentEvidence.text` 是单元 raw_text 用 `\n` 拼接，**不复现源文件空行**；
  逐字性按单元成立，但文档级文本在单元边界有歧义（源 `A\n\nB` → 文本 `A\nB`）。建议明写接口语义。

**改进建议（按 seam 位置排序）**：①给缺口一个**裁决 seam**（disposition：blocking / acknowledged 且留证）
或按 §7.3 分离「缺口记账」与「发布阻断」，让 scoped 可达且缺口仍可见；②**缺口坐标化**（补 page/char span）
使 scope 能判定其在范围外；③把**可发布性预检前移到 `corpus-plan`**（现仅校验清单格式），避免 build 成本已花；
④status/check 输出机器可读缺口；⑤read_pg 模块 docstring 补文档级拼接语义。

### 产出：全链路复核正式文档（2026-09-18，已落盘并校验链接）

- `audits/20260918-i2-fullchain-review/review.md`：F1—F4；§4 已成立 9 条链不变量；§6 覆盖核对（I2 验收门
  逐条 + 架构 §9 CLI 六职责逐条）；§7 三项裁定；§9 复现命令；§11 证据边界（明确 F2 为实测红、
  F1 为「实测 characterization + 规格/代码文本对照」，按 M8 分离观测与推理）。
- `audits/20260918-i2-fullchain-review/remediation-checklist.md`：编号 **`RM-FC-*`**
  （RM-FC-0 裁定前置；RM-FC-1 P1 缺口与发布门解耦；RM-FC-2 缺口坐标化；RM-FC-3/4 check/status
  缺口可见性；RM-FC-5 文档级语义；RM-FC-6 命名对齐；RM-FC-7 plan 预检前移；RM-FC-8 回路纳管+重冻+回填）。
  **三个 RM 命名空间并存**：`RM-*`（I2-5）、`RM-I28-*`（I2-8/I2-4）、`RM-FC-*`（全链路），文档内均声明不得混引。

**探针终版基线（写入 review.md §3）**：全链路回路 **10 passed / 2 failed**（2 红＝`check.gaps`、`status.gaps`）；
官方 I2 六族真库门 **71 passed 零 skip**；前三轮整改探针 **6 passed**；`validate_i0c_freeze.py` exit 0。
**重要方法细节**：`publish` 恒被拒那条探针断言的是「当前行为」故为**绿**（characterization），
不要误当成「F1 已修」——F1 的红不存在，因为没有可断言的显式契约（缺口无坐标 → scoped 无法自证）。


## I2-8 / I2-4 独立复核整改（F1—F11 / RM-I28-0～13）完成

复核落点 `audits/20260918-i2-8-i2-4-review/`（review.md + test_review_probes.py + remediation-checklist.md）；
基线探针 **5 failed / 1 passed** → 整改后 **6 passed**。

**裁定 RM-I28-0（我定，已写入架构 §7.2/§7.3 与 tasks.md）**
- 文档级读取 = **方案 A**：`document_text`/`source_resolver` 遵循句柄绑定的 build（与块级同语义），
  `DocumentEvidence.build_id` = 实际读取 build；撤销返回 `None`（非空串），零单元 build 才是 `""`。
- legacy 读路径 = **方案 A**：迁移目标上不可达——目标库含 corpus schema 时请求 `legacy` 即拒绝；
  `new` 在无 schema 库上亦拒绝；`auto` 仅作未迁移旧库过渡兜底。

**代码整改**
- `read_pg`：`_DOC_SQL` 改为按句柄 build；`fetch_document` 先核来源准入（非 in_scope → `WithdrawnError`）；
  coverage 补 `publication_snapshot_ref`（(source_id,active_build_id,generation) 全集 md5，与计数同一条 SQL）；
  新增 `search_with_coverage`（命中+覆盖同一 REPEATABLE READ 事务，§7.3 同快照）；`ChunkEvidence.spans`
  （每个 unit 在返回 text 内的偏移，可复算 text）。
- `search_pg`：抽出 `build_search_params`（校验+归一化唯一来源）与 `search_chunks_on(cur,...)`。
- `service`：`read_chain()` 严格化（拒绝非法取值/迁移目标 legacy/无 schema 的 new）；`search_with_coverage` 包装。
- `tools`：`corpus_search` 走组合读取；`corpus_fetch` 透出 `source_ranges`+`spans`；
  `data_coverage` 透出 `research_coverage`（三轴，与市场字段分层，verdict/guidance 不动）。
- `cli`：解析复用判定改调 `engine.expected_parse_rev`；退出码同因同码（check/status 的 build 不存在 = 5，
  门未过 = 4）；`main()` 捕获 argparse `SystemExit` → 2。
- `engine`：`_parse_rev_for` 为唯一公式落点 + 公开 `expected_parse_rev(source)`。

**文档登记**：旧引用 `doc_id→source_id` manifest **顺延 I4**；golden **carry-over I3-5**；
批处理 `scripts/corpus_holdout_eval.py`/`truncation_*.py`/`corpus_evidence_pilot.py` 归 **I3-5/I5-3**。

**证据**：真库门（含探针）**64 passed** 零 skip；普通环境语料全量 434 passed/1 skipped；i1 守卫 188 passed；
ruff（CI 范围）All checks passed；pyright `plugins/corpus plugins/tools` 0 errors；import_smoke 358/358；
冻结 **i0c-r11**（整改）+ **i0c-r12**（data_coverage 类型收窄，随包重绑）血缘校验通过；
证据矩阵 `audits/20260918-i2-8-i2-4-remediation/verification-matrix.txt`。

**教训（可复用）**：打包冻结后**任何字节变更都要新修订**——我因一处 pyright 类型收窄不得不补 r12；
冻结生成器应在最后一步跑（先 pyright/ruff 再冻）。另外守卫 env 下 `data_coverage` 会真发市场网络请求，
测试须桩掉 `build_service`。I2-8 由 partial 转 complete；**M5 仍 not_declared**（待 I2-6 + 独立复核）。


## I2-6（authority + cli_isolation）执行完成 —— I2 全部任务交付完毕

- **authority 族** `tests/test_corpus_authority_pg.py`（8 用例，真库）：所有消费者
  （fetch_verbatim / fetch / document_text / source_resolver / fetch_cell / 工具层 / 审计）
  读出**同一份** `corpus_units.raw_text`（逐项对照 ground truth）；坏哈希/篡改正文、引用悬空、
  错 cell、跨 build/坏/旧句柄、清洗视图与检索展示**不是**权威（逐字比对抓伪引文）、
  JSONB 副本与权威指纹不符拒绝、审计检出悬空引用。
- **cli_isolation 族** `tests/test_corpus_cli_isolation.py`（5 用例）：真实 CLI **子进程**
  （守卫经 subprocess.Popen 补丁注入同配置引导）走 build→check→publish(operator)→status、
  重启续跑幂等复用、退出码 2/3/5、openai import/构造被拒、非允许地址连接被拒而 127.0.0.1:543 放行、
  旧句柄 LegacyHandleError 不兜底。
- **本任务发现并修复的实质缺口**：读侧原先只在 chunk 路径核 `content_hash`，**文档级
  `fetch_document` 不核**——篡改 `corpus_units.raw_text` 会被当文档正文外流并被 verify 采用。
  现 chunk/文档/cell 三路都核哈希；引用悬空抛 `IntegrityError`（不再返回"其余部分"）。
- 新增接口：`read_pg.fetch_cell`（按 页/行/列 取权威单元格，错 cell 与多义拒绝）、
  `service.verify_evidence_against_authority`（证据副本 units 指纹须与权威集合一致）。
- 证据：真库门 **77 passed 零 skip**；普通环境规范 28 文件 **434 passed / 1 skipped**；
  i1 守卫 **188 passed**；ruff（CI 范围）/pyright(`plugins/corpus plugins/tools` 0 errors)/import_smoke 358/358；
  冻结 **i0c-r13**（parent=r12，validate 全过）。证据矩阵 `audits/20260918-i2-6/verification-matrix.txt`。
- **状态**：I2-1～I2-8 全部完成 → **M5 技术门条件已齐**（M3+M4+I2 全完成、publication_pg/authority/
  cli_isolation 真库零 skip）；按纪律**不自我宣告 M5**，待独立复核 + U 签认。下一步可考虑
  发起 M5 独立复核材料，或转入 I3（须先冻结 I3-2 预期/评分器，见 tasks.md §3.6）。


## M5 独立复核材料已备（申请方备料，非复核结论）—— 冻结 i0c-r14

落点 `.scratch/corpus-evidence-pipeline/ingestion-rebuild/audits/20260918-m5-review/`（4 件，已绑进 i0c-r14）：

- **README.md**：复核对象=i0c-r13（parent=r12）与版本锚点、放行条件核对清单 A—E（A 冻结链 / B 守卫与零模型 /
  C 必需测试族 17+18+8+5+19+4+6+188+19 / D 消费者接线与"旧来源不兜底" / E 已知未覆盖）、18 块命令矩阵与预期、
  已知口径差异、发现模板、纪律与判定输出（只允许"放行/阻断"两结论，不得以退出码单独放行）。
- **run_matrix.sh**：前端门 fail-fast（validate_i0c_freeze → 哈希留痕 → **target-guard 目标双校验** →
  guard selfcheck 24/24）；行为矩阵 9 块（真库 7 + i1 业务 + guard 测试）全 JUnit 出证；静态 4 块；
  运行后绑定零漂移门 + 哈希留痕；末尾 verify_matrix 汇总。evidence write-once，`M5_REVIEW_EVIDENCE_DIR` 仅供演练。
- **cross-check.md**：X1 哈希与血缘、X2 绑定逐文件复算+漏绑核查、X3 `--collect-only` 用例数一致、
  X4 守卫对抗探针、X5 write-once、X6 legacy/无 schema 双向 fail-closed、X7 同快照不变量、
  X8 零模型子进程、X9 目标 fail-closed、X10 装置自证。
- **verify_matrix.py**：退出码+JUnit 计数+日志命中+自检 JSON 自动判定；`--self-test` 篡改副本必须判 MISS。

**演练结果（申请方）**：18 块 MATRIX OK（零 failure/error/skip）、`--self-test` SELFTEST_OK（篡改判 2 项 MISS）、
重跑矩阵 exit=2、重发冻结生成器 exit=1、X4/X6/X8/X9 抽查命中；演练证据在 /tmp 已删除，不构成证据。

**下一步**：把本包交给全新会话的复核人（不得参与 I0—I2 制备）；复核人产出 `review.md`，
U 在总台账签认后才声明 M5。若 U 想先推进，I3-2（预期/评分器冻结）不依赖 M5 裁决但 I3-1 起需要 M5。

## I2 全链路复核整改（RM-FC-0～8）已实施完成（2026-09-18）

依据 `audits/20260918-i2-fullchain-review/`（review.md F1—F4 + remediation-checklist.md）。
**回路 10 passed / 2 failed → 12 passed**；官方六族真库门仍 **71 passed 零 skip**；
新增 `tests/test_corpus_gap_dispositions.py` 真库 **13 passed**；非 PG 语料全量 595 passed/5 skipped；
i1 守卫 189 passed/4 skipped；ruff（CI 范围，非全仓·全仓有 326 项既有告警）All passed；
pyright 全仓仅 `server/store.py` 19 项既有缺依赖；import_smoke 359/359 + 408/408；
冻结 **i0c-r15**（parent=r14，含 review_evidence 三件 + 自绑生成器/校验器），validate exit 0。

**裁定 RM-FC-0（我定，写入架构 §7.3/§7.2/§9）**：F1 = **A + C 组合**——缺口默认分级
（`blocking`/`acknowledged`）+ 缺口坐标化；判定顺序「scope 可证范围外 → 否则默认分级」。
F4 = **改规格用词**（`corpus-status --build`，§9 的 `--job` 作废）。
**关键取舍：不改 `quality_report` 形状**（仍两键），缺口身份沿用 `issue:<code>:<location>`，
解析集中在 `gaps.parse_gap_key` —— 这样官方六族测试文件**一个都不用改**（否则 5+ 个 frozen 测试要动）。

**代码落点**
- 新 `plugins/corpus/preparation/gaps.py`：`GAP_CODE_STATUS`/`DEFAULT_GAP_DISPOSITION`/
  `GAP_CODE_REMEDY` 三表（唯一词表来源，测试锁同键集）、`GapCoordinates`（page/element/char/
  unlocatable）、`GapLifecycle`（blocking/acknowledged/out_of_scope）、`evaluate_against_scope`、
  `gap_summary`、`recovery_paths`；词表外/键不可解析一律 blocking（fail-closed）。
- `clean.py`：`_SYNTHETIC_ISSUE_STATUS` 改为 `gaps.GAP_CODE_STATUS` 别名；`CleanRegion` 加
  `coordinates` 字段（无坐标显式 `unlocatable`）。
- `engine.py`：新增 `QualityLedger`/`quality_ledger_of`/`gap_records_of`；
  `_verify_publication_ready` 由「缺口非空恒拒」改为按裁决（错误文案仍含 `gap_regions`）；
  `_publish_record` 增 `gap_policy_rev` + `acknowledged_gaps`（记录人=operator）。
- `cli.py`：`check`（含被拒）/`status` 输出 `gaps`/`gap_summary`/`recovery`（台账不可读给
  `gaps_error`，不伪装成无缺口）；`plan` 增只读预检（同一分级实现，无 scope ⇒ 只可能更严）。
- `read_pg.py`：`_COVERAGE_SQL` 增 `published_with_gaps`（JSONB `->` + `jsonb_typeof` 安全判定，
  无需 cast）；活动 build 有缺口 ⇒ `processing=scoped` + `reason_codes=gap_regions_present`；
  `DocumentEvidence`/`fetch_document` docstring 写明 `"\n"` 拼接、非字节还原（F3）。

**踩坑（可复用）**
- **守卫 env 下 pytest `tmp_path`（/tmp）不可读**：守卫对 `open` 抛 OSError（fail-closed），
  新测试的合成来源必须写到仓库内（用 `.scratch/.../i2/tmp/`，与既有 PG 测试一致）。
- **CLI build 的 manifest 必须带 `review_decision_ids`**（空数组 ⇒ 准入 review_required ⇒
  outcome.build_id 为 None，实测踩到）。
- **engine 已完成 PARSED/CHUNKED**：`execute_builds` 后不要再 `_mark_stages_succeeded`，
  否则 `finish_job` 报 lease_lost。
- 全仓 `ruff check .` 有 326 项既有告警、`ruff format --check` 356 文件未格式化——**不是本轮引入**，
  CI 只 lint `frontier_agent/ apodex/ benchmarks/ workflows/ plugins/ deploy/ tools/ scripts/`（该范围全绿）。
- `validate_i1_freeze.py` 对 i1-r3 的工作区绑定自 I0-C/I2 起已失配（**既有状态，非本轮引入**）；
  M5 复核矩阵只要求 `validate_i0c_freeze.py`，已在 r15 notes 登记。
- 冻结后**任何字节变更都要新修订**：原想再给 `status` 加 `publishable`/`gate_error`，因 r15 已
  write-once 而放弃（避免无谓 r16）；改动只能发生在冻结之前。

## M5 独立复核执行完毕 + U 签认（2026-09-18）—— M5 放行

用户指令「独立复核 + U 签认」。落点 `audits/20260918-m5-review/`：
**review.md**（复核报告）+ **evidence/**（矩阵 20 块原始产物，write-once）+ **cross-check-evidence.txt**（X1—X15）
+ **evidence-postfix/recheck.txt**（F1 修复后复验）+ **evidence-aborted-r15-pregate/**（首次被拦产物）。
结论：**M5 放行**（矩阵 20 块 MATRIX OK 零 failure/error/skip；X1—X15 全过；装置自证 SELFTEST_OK）。

**关键流程事实（可复用）**
- M5 复核包 v1 锚 i0c-r13；r15 改了实现字节后 v1 过时 → 申请方须**重发包 v2**（改绑 r15、新增两个链级块、
  `import-smoke` 判据由固定 `358/358` 改为**导入闭合正则** `(\d+)/\1 modules imported`，不放宽任何既有块计数）
  **并建新修订 r16 重绑包字节**；否则矩阵**前置门 freeze-validator 直接 exit=1**（实测：绑定零漂移门有效）。
- 复核执行者 = 本会话（I0—I2 制备方），**不满足包 §9「全新会话」**：已在 review.md §0 显著登记独立性偏差
  并由 U 签认时接受；若 U 反悔 → M5 立即回 `not_declared` 另派复核人（材料已就绪，重跑约 5 分钟）。
- **复核自身发现 2 项缺陷并当轮闭环**：F1（P2 测试卫生）`test_corpus_gap_dispositions.py` 直接
  `json.loads(stdout)` 被 pymupdf 一次性提示行污染 → 单独运行/被 `-k` 选中即**顺序依赖假红**；
  修 `_cli_json()` 从首个 `{` 解析；F2（P3）备料包锚点过时（见上）。F3（P3）`validate_i1_freeze.py`
  对工作区失配属既有状态（M5 矩阵只用 `validate_i0c_freeze.py`）。
- **冻结链最终 = i0c-r18**：r15（全链路整改）→ r16（复核包 v2 重绑）→ r17（复核报告 + 证据面 +
  F1 修复字节 + 台账签认）→ r18（补记报告冻结修订标识）。`validate_i0c_freeze.py` exit 0；
  重跑 `verify_matrix.py` 后 matrix-summary.json 字节不变（已绑定，验证过）。
- **测试新坑**：任何「库可能往 stdout 打提示行」的场景，测试不得直接 `json.loads(capsys 输出)`——
  会因进程内是否已打印过而呈现顺序依赖；统一从首个 `{` 解析。
- M5 签认只覆盖 PG 门与消费者接线；**I3 仍须先做 I3-0/I3-2**（评分器与预期冻结），I3-1 另需其 F1 前置（已闭环）。
