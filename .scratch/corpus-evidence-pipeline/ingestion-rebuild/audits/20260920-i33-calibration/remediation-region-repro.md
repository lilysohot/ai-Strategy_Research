# FIX-3 区域隔离只读预检结论（Stage-1 part 2）

- 生成：2026-09-20（只读；不改 PG/金标/评分器/版本号；未写回 `evidence-diagnosis.json`）
- 脚本：`repro_region_isolation.py` → `remediation-region-repro.json`
- 依据：guide §5 FIX-3「页眉/页脚/图例/侧栏区域隔离」，输出顺序固定；数据源 = 6 份真实开发 PDF。

## 1. 行级策略对比（34 条引文去空白后连续命中）

| 策略 | hit | miss |
|---|---:|---:|
| before（当前单栏流 `(y,x)` 排序） | 12 | 22 |
| center（guide FIX-1：x 中心 gap 聚类） | 14 | 20 |
| **x0（x 左边缘聚类分侧栏）** | **17** | **17** |
| x0 + 页眉/页脚 y 带隔离 | 17 | 17 |

## 2. 结论

1. **x0 左边缘聚类是侧栏分离的正确信号**：右栏作者框/`相关研究报告`/图例的 x0≈394–524，
   正文 x0≈41，左边缘干净分群；优于 guide 的 x 中心聚类（x 中心被窄左行/中间元素填密）。
2. 但最优区域隔离最多把 miss 22→17，**net 仅 ~5 条**；页眉/页脚带隔离零增益。
3. **17 条在不动金标/评分器/放松判据的前提下，本质上不可由阅读顺序/区域隔离修复**
   ——重摄入至多 miss 22→17，距 guide §6 的 34→≤2 差 15 条。

## 3. 判定

**不值得 bump `READER_PDF_REV` + 全量重摄入 + 冻结链重绑**（为 ~5 条付出整链代价，且 17 条
仍非连续）。回归当前基线（代码未改）。此前也已回退 FIX-1 试落改动（`git diff` 空）。
本簇证据保留结论：接受「摄入侧部分有效(≈5 需重摄入但放弃) + 匹配侧空白等价(已合入) +
不可达分级账」收口，待 U 确认终结后转入有界召回 / 负例策略。

## 4. 红线

全程只读：未改金标/评分器/locator、未改单元集合、未写 PG、未 bump 版本、未就地写
`evidence-diagnosis.json`。不因本预检宣称 I3-3/M6 放行。