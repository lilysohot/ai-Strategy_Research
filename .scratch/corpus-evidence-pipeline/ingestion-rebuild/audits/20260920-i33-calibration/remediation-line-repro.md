# Stage-1 预检：行级只读重排验证结论（FIX-1 可行性被证伪）

- 生成：2026-09-20（只读；不改 PG/金标/评分器/版本号；未写回 `evidence-diagnosis.json`）
- 脚本：`repro_line_column.py` → `remediation-line-repro.json`
- 依据：guide §5 FIX-1「x 中心一维聚类分栏」；数据源 = 6 份真实开发 PDF 经 sha256 匹配。

## 1. 问题复述

guide §1 断言 34 条中「32 条可在不动金标、不放松判据前提下靠摄入侧修复直接转绿」，且 FIX-1（页内阅读顺序重建：栏聚类）为 P0 主因（25 条）。Stage-1 需据此改 reader + 全量重摄入。

## 2. 行级只读量测（本验证）

直接读真实 PDF 页，按 reader 的 `_merge_lines` 段落装配，分别模拟：
- `before`：当前单栏流（全行按 `(y,x)` 排序）
- `after`：guide 建议的 x 中心聚类分栏（阈值 `(content_w)*0.18`，spanner 分离）

对 34 条引文做去空白后连续命中统计：

| 路径 | hit | miss |
|---|---:|---:|
| before（单栏流，fresh 行级） | 12 | 22 |
| after（x 中心聚类） | 14 | 20 |
| guide §6 目标 | — | **≤2** |

## 3. 关键证伪点

1. **聚类净恢复仅 ~2 条**（22→20 miss），距 ≤2 差 18 条。
2. company-001 / e4（guide §2.1 的现场 A）在 `after` **仍未修复**：`相关研究报告` 依旧夹在「我们维持26-28年EPS预测值」与「67.74/…评级」之间。
3. 根因：company-001 第 1 页 `content_w≈566`，阈值≈102，但页内 x 中心 24→575 **连续分布**，相邻 center gap 全部 <102 → **只有 1 栏**；`相关研究报告`（center≈424）与左栏正文（center≈195）被归入同一簇，全局聚类无法分离浮动侧栏。
4. 说明：guide FIX-1 的「x 中心一维聚类」在该批次 PDF 的稠密排版下失效；「32 条靠摄入侧修复直接转绿」在行级层面不可复现。

## 4. 结论

- `README` 顺序（先 34→≤2 再谈有界召回）不变，但 **「34→≤2 可由摄入侧修复直接达到」的前提在本批次数据上不成立**。
- 在前提被证伪的情况下，bump `READER_PDF_REV` + 全量重摄入 + 冻结链重绑是高成本低回报，**不建议盲目执行**。
- 待 U 裁决后再决定：是否要求更细的版面模型（页眉/页脚/图例/侧栏的显式区域隔离 FIX-3），或接受「部分可由摄入侧修复、其余须匹配侧等效或人工诊断」的分级账。

## 5. 红线

全程只读：未改金标/评分器/locator、未改单元集合、未写 PG、未 bump 版本、未就地写 `evidence-diagnosis.json`。不因本验证宣称 I3-3/M6 放行。